import { StyleSheet } from 'react-native';

const HomeStyles = StyleSheet.create({
    content: {
        padding: 20,
    },
    header: {
        textAlign: 'center',
        fontSize: 20,
        marginBottom: 20,
    },
});

export default HomeStyles;
